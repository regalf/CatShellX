# Gallery

Screenshots of CatShellX in action (taken on Mac OS X Tiger / TerminalX).

Drop PNG/JPEG files here and reference them from the README with a
relative path:

```markdown
![Alt text](gallery/name.png)
```

Ideas: the fish-like prompt, autosuggestions, syntax highlighting,
`cat_config` screens (prompt preview, greetings, startup commands).
